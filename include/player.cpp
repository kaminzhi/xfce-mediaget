#ifndef PLAYER_HPP
#define PLAYER_HPP

#include <string>

struct PlayerMetadata {
    std::string status;
    std::string artist;
    std::string title;
    std::string player;
    std::string time;
    std::string art_url;
};

#endif
